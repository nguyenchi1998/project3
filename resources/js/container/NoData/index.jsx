import { Typography } from '@mui/material';

const NoData = () => {
  return (
    <Typography textAlign="center" variant="h5">
      No data to display
    </Typography>
  );
};

export default NoData;
